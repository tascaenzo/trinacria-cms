import { Meta, Canvas } from "@storybook/addon-docs/blocks";
import * as Stories from "./admin-shell.stories";

<Meta of={Stories} />

# AdminShell

`AdminShell` e il frame strutturale del backoffice: sidebar, header, breadcrumb operativo e area contenuti.

<Canvas of={Stories.Default} />
