import { Meta, Canvas } from "@storybook/addon-docs/blocks";
import * as Stories from "./json-view.stories";

<Meta of={Stories} />

# JsonView

`JsonView` fornisce un pannello di ispezione grezzo per payload runtime, debug e configurazioni.

<Canvas of={Stories.Payload} />
