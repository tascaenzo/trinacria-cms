import { Meta, Canvas } from "@storybook/addon-docs/blocks";
import * as Stories from "./toast.stories";

<Meta of={Stories} />

# Toast

Provider e viewport per notifiche transitorie con dismiss e durata configurabile.

<Canvas of={Stories.Playground} />
