import { Meta, Canvas } from "@storybook/addon-docs/blocks";
import * as Stories from "./page-section.stories";

<Meta of={Stories} />

# Page Section

`PageHeader` e `ActionBar` definiscono i blocchi ricorrenti di apertura e controllo delle pagine backoffice. `ContentSection` è la variante lineare, senza card, per form e documenti lunghi.

<Canvas of={Stories.ResourceHeader} />

<Canvas of={Stories.LinearDocumentSections} />
