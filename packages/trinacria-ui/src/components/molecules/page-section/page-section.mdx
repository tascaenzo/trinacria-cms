import { Meta, Canvas } from "@storybook/addon-docs/blocks";
import * as Stories from "./page-section.stories";

<Meta of={Stories} />

# Page Section

`PageHeader` e `ActionBar` definiscono i blocchi ricorrenti di apertura e controllo delle pagine backoffice.

<Canvas of={Stories.ResourceHeader} />
