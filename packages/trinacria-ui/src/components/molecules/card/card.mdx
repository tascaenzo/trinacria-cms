import { Meta, Canvas } from "@storybook/addon-docs/blocks";
import * as Stories from "./card.stories";

<Meta of={Stories} />

# Card

`Card` definisce la superficie standard per moduli, pannelli di dettaglio e blocchi riepilogativi del backoffice.

<Canvas of={Stories.Composition} />
