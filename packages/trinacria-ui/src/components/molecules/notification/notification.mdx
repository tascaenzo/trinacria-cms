import { Meta, Canvas } from "@storybook/addon-docs/blocks";
import * as Stories from "./notification.stories";

<Meta of={Stories} />

# Notification

Messaggio inline dismissable con varianti semantiche per stati operativi e feedback persistenti.

<Canvas of={Stories.States} />
