import { Meta, Canvas } from "@storybook/addon-docs/blocks";
import * as Stories from "./pagination.stories";

<Meta of={Stories} />

# Pagination

Navigazione per tabelle e liste con range record, numeri pagina, stato corrente e selettore righe per pagina opzionale.

<Canvas of={Stories.Default} />
