import { Meta, Canvas } from "@storybook/addon-docs/blocks";
import * as Stories from "./filter-bar.stories";

<Meta of={Stories} />

# Filter Bar

Wrapper per filtri e azioni ricorrenti nelle pagine elenco del backoffice.

<Canvas of={Stories.Default} />
