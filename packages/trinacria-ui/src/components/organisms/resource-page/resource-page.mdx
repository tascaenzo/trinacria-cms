import { Meta, Canvas } from "@storybook/addon-docs/blocks";
import * as Stories from "./resource-page.stories";

<Meta of={Stories} />

# Resource Page

Wrapper pagina standard per schermate admin con header, toolbar, feedback stack e sidebar opzionale.

<Canvas of={Stories.Default} />
