import { Meta, Canvas } from "@storybook/addon-docs/blocks";
import * as OverlaySurfaceStories from "./overlay-surface.stories";

<Meta of={OverlaySurfaceStories} />

# Overlay Surface

`OverlaySurface` centralizza bordo, radius, background e shadow per popover, dialog, drawer,
menu e picker del backoffice.

<Canvas of={OverlaySurfaceStories.Variants} />
