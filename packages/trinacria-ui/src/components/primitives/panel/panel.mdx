import { Meta, Canvas } from "@storybook/addon-docs/blocks";
import * as Stories from "./panel.stories";

<Meta of={Stories} />

# Panel

Primitive di superficie per blocchi contenitivi, wrapper composti e stati vuoti.

<Canvas of={Stories.Variants} />
