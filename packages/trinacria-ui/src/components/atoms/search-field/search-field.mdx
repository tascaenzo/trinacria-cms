import { Meta, Canvas } from "@storybook/addon-docs/blocks";
import * as Stories from "./search-field.stories";

<Meta of={Stories} />

# Search Field

Campo di ricerca compatto per shell, toolbar e pannelli filtro del backoffice.

<Canvas of={Stories.Default} />
