import { Meta, Canvas } from "@storybook/addon-docs/blocks";
import * as Stories from "./input.stories";

<Meta of={Stories} />

# Input

`Input` copre il caso standard dei campi testuali del backoffice con hint, errori e disabled state.

<Canvas of={Stories.States} />
