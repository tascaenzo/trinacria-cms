import { Meta, Canvas } from "@storybook/addon-docs/blocks";
import * as Stories from "./textarea.stories";

<Meta of={Stories} />

# Textarea

`Textarea` e usato per contenuti strutturati, descrizioni e payload modificabili con validazione inline.

<Canvas of={Stories.States} />
