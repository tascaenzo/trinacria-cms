import { Meta, Canvas } from "@storybook/addon-docs/blocks";
import * as Stories from "./switch.stories";

<Meta of={Stories} />

# Switch

`Switch` e adatto ai toggle di configurazione che modificano uno stato persistente o una policy del sistema.

<Canvas of={Stories.States} />
