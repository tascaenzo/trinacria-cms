import { Meta, Canvas } from "@storybook/addon-docs/blocks";
import * as Stories from "./select.stories";

<Meta of={Stories} />

# Select

`Select` copre i controlli a scelta singola del backoffice mantenendo gli stessi token dei campi testuali.

<Canvas of={Stories.States} />
