import { Meta, Canvas } from "@storybook/addon-docs/blocks";
import * as Stories from "./icon-button.stories";

<Meta of={Stories} />

# IconButton

`IconButton` e l'atomo per azioni compatte di toolbar, table rows e controlli contestuali.

Usalo solo quando il significato resta chiaro con una label accessibile esplicita.

<Canvas of={Stories.Variants} />
<Canvas of={Stories.Sizes} />
