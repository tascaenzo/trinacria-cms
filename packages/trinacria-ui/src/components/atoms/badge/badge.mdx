import { Meta, Canvas } from "@storybook/addon-docs/blocks";
import * as Stories from "./badge.stories";

<Meta of={Stories} />

# Badge

`Badge` espone stati sintetici e semantici per runtime, record e indicatori operativi.

<Canvas of={Stories.Tones} />
