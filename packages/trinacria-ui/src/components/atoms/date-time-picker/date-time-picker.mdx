import { Meta, Canvas } from "@storybook/addon-docs/blocks";
import * as Stories from "./date-time-picker.stories";

<Meta of={Stories} />

# Date Time Picker

`DateTimePicker` combina calendario e selettore orario per scheduling, finestre operative e deadline di sistema.

<Canvas of={Stories.States} />
