import { Meta, Canvas } from "@storybook/addon-docs/blocks";
import * as Stories from "./field.stories";

<Meta of={Stories} />

# Field

`Field` coordina label, descrizione, hint ed errori per comporre controlli consistenti.

<Canvas of={Stories.Composition} />
