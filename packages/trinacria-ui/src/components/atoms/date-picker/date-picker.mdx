import { Meta, Canvas } from "@storybook/addon-docs/blocks";
import * as Stories from "./date-picker.stories";

<Meta of={Stories} />

# Date Picker

`DatePicker` e il calendario custom del design system, utile quando il controllo nativo non basta in termini di coerenza visiva e UX.

<Canvas of={Stories.States} />
