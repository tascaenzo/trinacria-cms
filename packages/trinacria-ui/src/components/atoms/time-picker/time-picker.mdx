import { Meta, Canvas } from "@storybook/addon-docs/blocks";
import * as Stories from "./time-picker.stories";

<Meta of={Stories} />

# Time Picker

`TimePicker` sostituisce il controllo orario nativo con un pannello coerente col design system e con step configurabile.

<Canvas of={Stories.States} />
